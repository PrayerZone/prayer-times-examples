# Vanilla HTML widget

Open `index.html` directly or serve the directory with any static web server.

The example pins the PrayerZone Web Component to `v2.0.1` and requires no build
step or framework.
