# React

Copy `PrayerTimesWidget.jsx` into a React application and render it like this:

```jsx
<PrayerTimesWidget city="paris" language="fr" showQibla />
```

The wrapper loads the versioned Web Component once and maps React props to custom
element attributes.
