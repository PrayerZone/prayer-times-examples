# Browser API dashboard

This dependency-free page fetches city prayer times from the public API and renders
a compact table with loading and error states.

Serve the directory with a local HTTP server:

```bash
python -m http.server 8080 --directory examples/browser-api
```

Then open `http://localhost:8080`.
